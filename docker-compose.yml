version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app
    environment:
      - user=postgres.syuyrduryrlvlkrnmyzp
      - password=112233445566778899qwe
      - host=aws-0-ap-south-1.pooler.supabase.com
      - port=5432
      - dbname=postgres
    depends_on: []

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    environment:
      - REACT_APP_API_URL=http://localhost:8000
    depends_on:
      - backend 